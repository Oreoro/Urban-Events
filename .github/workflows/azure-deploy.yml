# .github/workflows/azure-deploy.yml
name: Build and Deploy to Azure Web App

# This workflow runs on pushes to the development branch or can be triggered manually.
on:
  push:
    branches:
      - development
  workflow_dispatch:

jobs:
  build-and-deploy:
    name: Build and Deploy
    runs-on: ubuntu-latest

    steps:
      # Checkout the repository code
      - name: 'Checkout Repository'
        uses: actions/checkout@v4

      # Authenticate to your Azure account using a service principal.
      - name: 'Log in to Azure'
        uses: azure/login@v1
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}

      # Authenticate to the Azure Container Registry.
      - name: 'Log in to Azure Container Registry'
        run: az acr login --name ${{ secrets.ACR_LOGIN_SERVER }}

      # Build and push the Docker image to ACR.
      - name: 'Build and Push Docker image'
        env:
          ACR_LOGIN_SERVER: ${{ secrets.ACR_LOGIN_SERVER }}
        run: |
          echo "Building and pushing Docker image..."
          # Build the Docker image. The compose service tags it as urban-events:v1.
          docker compose -f ${{ github.workspace }}/docker/all-in-one/docker-compose.yml build
          
          # Tag the locally built image with the full ACR registry path.
          docker tag urban-events:v1 ${{ secrets.ACR_LOGIN_SERVER }}/urban-events:v1
          
          # Push the newly tagged image to ACR.
          docker push ${{ secrets.ACR_LOGIN_SERVER }}/urban-events:v1
          echo "✅ Docker image built and pushed."

      # Upload application settings to the Azure Web App.
      - name: 'Set App Service Settings'
        uses: azure/CLI@v1
        env:
          AZURE_APP_SECRETS: ${{ secrets.AZURE_APP_SECRETS }}
          PUBLIC_FRONTEND_URL: ${{ vars.PUBLIC_FRONTEND_URL || 'https://app.urbanevents.pk' }}
          PUBLIC_API_URL_CLIENT: ${{ vars.PUBLIC_API_URL_CLIENT || 'https://app.urbanevents.pk/api' }}
        with:
          inlineScript: |
            echo "Setting Azure App Service application settings..."

            python3 <<'PY'
            import json
            import os

            settings = []

            for raw_line in os.environ.get("AZURE_APP_SECRETS", "").splitlines():
                line = raw_line.strip()

                if not line or line.startswith("#"):
                    continue

                if line.startswith("export "):
                    line = line[len("export "):].strip()

                if "=" not in line:
                    continue

                name, value = line.split("=", 1)
                name = name.strip()
                value = value.strip()

                if not name:
                    continue

                if len(value) >= 2 and value[0] == value[-1] and value[0] in ("'", '"'):
                    value = value[1:-1]

                settings.append({
                    "name": name,
                    "value": value,
                    "slotSetting": False,
                })

            production_overrides = {
                "VITE_API_URL_CLIENT": os.environ["PUBLIC_API_URL_CLIENT"],
                "VITE_FRONTEND_URL": os.environ["PUBLIC_FRONTEND_URL"],
                "ENV_VITE_API_URL_CLIENT": os.environ["PUBLIC_API_URL_CLIENT"],
                "ENV_VITE_FRONTEND_URL": os.environ["PUBLIC_FRONTEND_URL"],
            }

            settings_by_name = {setting["name"]: setting for setting in settings}

            for name, value in production_overrides.items():
                settings_by_name[name] = {
                    "name": name,
                    "value": value,
                    "slotSetting": False,
                }

            settings = list(settings_by_name.values())

            with open("appsettings.json", "w", encoding="utf-8") as handle:
                json.dump(settings, handle)

            print(f"Prepared {len(settings)} app settings.")
            PY

            az webapp config appsettings set \
              --resource-group ${{ secrets.AZURE_RESOURCE_GROUP }} \
              --name ${{ secrets.AZURE_WEBAPP_NAME }} \
              --settings @appsettings.json \
              --output none
            
            echo "✅ Application settings configured."

      # Deploy the new Docker image to the Azure Web App.
      - name: 'Deploy to Azure Web App'
        uses: azure/webapps-deploy@v2
        with:
          app-name: ${{ secrets.AZURE_WEBAPP_NAME }}
          # This image name MUST match the one pushed in the previous step.
          images: '${{ secrets.ACR_LOGIN_SERVER }}/urban-events:v1'

      # Log out of Azure as a security best practice.
      - name: 'Log out of Azure'
        if: always()
        run: |
          az logout
          az cache purge
          az account clear
